# Boer.cn 内容迁移工具集

这是用于从 boer.cn 手动迁移内容到 Web-For-HK 项目的工具集和说明文档。

## 📁 目录结构

```
migration-assets/
├── templates/                      # Excel/CSV 收集模板
│   └── product-collection-template.xlsx  # 产品信息收集表（待创建）
├── collected-data/                 # 填写后的数据文件
│   └── new-products-example.json   # 示例产品数据
├── downloaded-images/              # 下载的原始图片
│   └── temp/                       # 临时存放下载的图片
├── scripts/                        # 数据处理脚本
│   ├── package.json                # NPM 配置
│   ├── convert-traditional.ts      # 简繁转换脚本
│   ├── generate-products.ts        # 产品数据生成脚本
│   └── update-mockdata.ts          # mockData.ts 更新脚本
└── translations/                   # 分语言的翻译文件
    ├── zh-CN/                      # 简体中文
    ├── zh-HK/                      # 繁体中文（香港）
    └── en/                         # 英文
```

## 🚀 快速开始

### 第一步：收集产品数据

1. **访问 boer.cn 产品页面**: https://boer.cn/products

2. **手动复制产品信息**到 Excel 表格（或直接创建 JSON 文件）：

   必填字段：
   - `product_id`: 产品唯一标识（如 bc440g3-p）
   - `source_url`: boer.cn 页面 URL
   - `category`: 产品分类（server/storage/hpc/storage-hp/network）
   - `name_zhCN`: 产品名称（简体中文）
   - `description_zhCN`: 产品描述（简体中文）
   - `image_filename`: 图片文件名（如 bc440g3-p.png）
   - `featured`: 是否首页展示（true/false）

   可选字段：
   - `specs_model`: 型号
   - `specs_cpu`: 处理器
   - `specs_memory`: 内存
   - 等等...

3. **下载产品图片**:
   - 在产品详情页右键点击图片 → "图片另存为"
   - 保存到 `migration-assets/downloaded-images/temp/`
   - 重命名为 `{product_id}.png`（如 bc440g3-p.png）

### 第二步：安装脚本依赖

```bash
cd migration-assets/scripts
npm install
```

这将安装以下依赖：
- `opencc-js`: 简繁转换库
- `tsx`: TypeScript 执行工具

### 第三步：准备数据文件

将收集的数据保存为 JSON 格式到 `collected-data/new-products.json`：

```json
[
  {
    "product_id": "bc440g3-p",
    "source_url": "https://boer.cn/products/bc440g3-p",
    "category": "hpc",
    "name_zhCN": "BC440G3-P 高性能计算服务器",
    "description_zhCN": "2U高性能计算服务器，适用于科学计算...",
    "specs_model": "BC440G3-P",
    "specs_cpu": "AMD EPYC 9004系列处理器",
    "specs_memory": "24x DDR5-4800 DIMM插槽",
    "specs_storage": "热插拔 NVMe SSD",
    "image_filename": "bc440g3-p.png",
    "featured": true
  }
]
```

参考示例文件：`collected-data/new-products-example.json`

### 第四步：复制产品图片

```bash
# 将下载的图片复制到正式目录
cp migration-assets/downloaded-images/temp/*.png public/images/products/
```

### 第五步：运行更新脚本

```bash
cd migration-assets/scripts

# 方式 1: 从 JSON 文件更新
npx tsx update-mockdata.ts ../collected-data/new-products.json

# 方式 2: 在代码中调用（需要修改 update-mockdata.ts）
```

脚本将自动：
- ✅ 备份原文件到 `utils/mockData.ts.backup`
- ✅ 生成符合格式的 Product 对象
- ✅ 自动转换简体中文到繁体中文（香港）
- ✅ 添加英文占位符（需要人工翻译）
- ✅ 追加到 `utils/mockData.ts`

### 第六步：翻译英文内容

打开 `utils/mockData.ts`，搜索 `[TRANSLATE]`，替换为实际的英文翻译：

```typescript
// 之前 / Before
en: '[TRANSLATE] BC440G3-P 高性能计算服务器'

// 之后 / After
en: 'BC440G3-P High-Performance Computing Server'
```

建议使用 DeepL 或 Google Translate 辅助翻译。

### 第七步：测试验证

```bash
npm run dev
```

访问 http://localhost:3000/products 验证新产品是否正确显示。

## 📝 Excel 模板说明

创建 Excel 文件 `templates/product-collection-template.xlsx`，包含以下列：

| 列名 | 说明 | 示例 | 必填 |
|------|------|------|------|
| product_id | 产品ID（小写英文+连字符） | bc440g3-p | ✅ |
| source_url | boer.cn页面URL | https://boer.cn/products/xxx | ✅ |
| category | 产品分类 | hpc | ✅ |
| name_zhCN | 产品名称（简体中文） | BC440G3-P 高性能计算服务器 | ✅ |
| description_zhCN | 产品描述（简体中文） | 2U高性能计算服务器... | ✅ |
| specs_model | 型号 | BC440G3-P | |
| specs_cpu | 处理器 | AMD EPYC 9004系列 | |
| specs_memory | 内存 | 24x DDR5-4800 | |
| specs_storage | 存储 | 热插拔 NVMe | |
| image_filename | 图片文件名 | bc440g3-p.png | ✅ |
| featured | 是否首页展示 | true | ✅ |

填写完成后，将 Excel 内容复制并保存为 JSON 格式。

## 🛠️ 脚本说明

### convert-traditional.ts

简体中文转繁体中文（香港）工具。

```bash
# 运行示例
npx tsx convert-traditional.ts
```

**功能**:
- 将简体中文转换为繁体中文（香港）
- 支持批量转换产品数组
- 保留专业术语（PCIe、InfiniBand 等）

### generate-products.ts

将原始数据转换为 Product 接口格式。

```bash
# 运行示例
npx tsx generate-products.ts
```

**功能**:
- 生成符合 TypeScript 类型的 Product 对象
- 自动生成 slug（URL 友好 ID）
- 自动转换简繁体

### update-mockdata.ts

更新 `utils/mockData.ts` 文件。

```bash
# 从 JSON 文件更新
npx tsx update-mockdata.ts path/to/new-products.json
```

**功能**:
- 备份原文件
- 追加新产品到 mockProducts 数组
- 自动转义特殊字符

## 📋 产品分类映射

| boer.cn 分类 | category 值 | 说明 |
|-------------|-------------|------|
| 通用计算 | `server` | 通用计算服务器 |
| 通用存储 | `storage` | 通用存储服务器 |
| 高性能计算 | `hpc` | 高性能计算服务器 |
| 高性能存储 | `storage-hp` | 高性能存储服务器 |
| IB交换机 | `network` | InfiniBand 交换机 |
| 以太网交换机 | `network` | 以太网交换机 |

## ✅ 检查清单

### 数据收集阶段
- [ ] 访问所有产品页面
- [ ] 收集产品基本信息（名称、描述、分类）
- [ ] 收集技术规格（CPU、内存、存储等）
- [ ] 记录产品页面 URL
- [ ] 下载产品图片
- [ ] 按命名规范重命名图片

### 数据处理阶段
- [ ] 创建 JSON 数据文件
- [ ] 验证 JSON 格式正确
- [ ] 复制图片到 `public/images/products/`
- [ ] 运行更新脚本
- [ ] 检查 mockData.ts 更新成功

### 翻译阶段
- [ ] 自动转换 zh-CN → zh-HK
- [ ] 校对繁体中文（香港用词）
- [ ] 翻译英文内容
- [ ] 检查专业术语一致性
- [ ] 移除所有 `[TRANSLATE]` 占位符

### 测试阶段
- [ ] 启动开发服务器
- [ ] 访问产品列表页
- [ ] 检查新产品显示
- [ ] 点击查看产品详情
- [ ] 验证图片加载
- [ ] 切换语言测试
- [ ] 测试移动端显示

## 🔧 常见问题

### Q1: 如何处理多个产品图片？

**A**: 使用以下命名规范：
```
bc440g3-p-01.png  (主图)
bc440g3-p-02.png  (第二张图)
bc440g3-p-03.png  (第三张图)
```

在 JSON 中使用数组：
```json
{
  "image_filenames": ["bc440g3-p-01.png", "bc440g3-p-02.png"]
}
```

### Q2: 简繁转换不准确怎么办？

**A**:
1. `opencc-js` 提供了基础的自动转换
2. 可能需要人工校对和修正
3. 特别注意：
   - "服务器" → "伺服器"
   - "软件" → "軟件"
   - "信息" → "信息"（保持不变）

### Q3: 英文翻译占位符还在？

**A**: 需要人工翻译。建议：
1. 使用 DeepL（https://www.deepl.com/translator）
2. 或使用 Google Translate
3. 人工校对专业术语

### Q4: 产品规格字段不够用？

**A**: 可以添加自定义字段：
```json
{
  "specs_custom_field": "自定义值"
}
```

脚本会自动处理所有 `specs_` 开头的字段。

### Q5: 如何撤销更新？

**A**: 脚本会自动创建备份文件：
```bash
# 恢复备份
cp utils/mockData.ts.backup utils/mockData.ts
```

或使用 Git：
```bash
git checkout utils/mockData.ts
```

## 📚 参考资料

### 项目相关文件
- [utils/mockData.ts](../../utils/mockData.ts) - 产品数据主文件
- [types/index.ts](../../types/index.ts) - TypeScript 类型定义
- [public/images/products/](../../public/images/products/) - 产品图片目录

### 外部工具
- [DeepL 翻译](https://www.deepl.com/translator) - 高质量翻译
- [Google Translate](https://translate.google.com/) - 在线翻译
- [OpenCC](https://github.com/BYVoid/OpenCC) - 开源简繁转换

## 🔄 后续维护

当 boer.cn 有新产品时，重复以上流程即可。

建议创建 Git 分支进行更新：
```bash
git checkout -b feat/add-new-products
# ... 执行迁移流程 ...
git add .
git commit -m "feat: add new products from boer.cn"
git push origin feat/add-new-products
# 创建 Pull Request
```

## 💡 提示

1. **数据质量**: 确保从 boer.cn 复制的文字准确无误
2. **图片清晰度**: 下载高分辨率的产品图片
3. **翻译一致性**: 使用相同的专业术语翻译
4. **版本控制**: 及时提交 Git，保留历史记录
5. **备份重要**: 脚本会自动备份，但建议手动备份重要数据

## 📞 支持

如有问题，请查看：
- 项目 README.md
- 计划文档：`C:\Users\TX\.claude\plans\woolly-dancing-parrot.md`

---

**最后更新**: 2025-01-26
**版本**: 1.0.0
