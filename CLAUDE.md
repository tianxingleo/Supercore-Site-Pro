# Agent Guidelines

## Tool Usage Priorities

1. **Search Preference**: 当你需要查阅 Nuxt 3 文档、Supabase API 或最新的编程技术时，**必须优先使用 `tavily-search`**（或者说search）。
2. **Fallback**: 只有当 Tavily 报错或返回结果为空时，才允许使用其他搜索mcp。
